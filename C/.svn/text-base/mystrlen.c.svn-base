#include <stdio.h>

char str1[] = "ShopEx OSS Test.";
//char str2 = "ShopEx Web Service Test.";

int mystrlen(char *str);


int main()
{
  printf("Mystrlen(str1) = %d\n",mystrlen(str1));
  
  return 0;
}

int mystrlen(char *str)
{
   int strlen = 0;
   //char *len = (char *)str; 
   while(*(str++) != '\0'){
   //while(*str!= '\0'){
    strlen++;
    printf("strlen = %d\n",strlen);
    sleep(1);
   } 

  return strlen;
}
